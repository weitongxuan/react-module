import { Icon, Input } from 'antd';
import _ from 'lodash';
import React, { Component } from 'react';

interface OverviewPageProps { }

export default class OverviewPage extends Component<OverviewPageProps, any> {

  public render() {

    return (
      <div>

      </div>
    );
  }
}
